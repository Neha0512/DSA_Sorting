//Naive Partition algorithm is stable but comparatively slower than other partition algo.  
// Time Complexity is 0(n) and takes 0(n) extra space

#include<bits/stdc++.h>
using namespace std;

int naivePart(int arr[], int l, int p, int h)
{
	//create temp arr;
	int temp[h-l+1];
	int index=0;
	
	//comparisons
	for(int i=0; i<p; i++)
	{
		if(arr[i]<arr[p])
		{
			arr[i]
		}
	}
}
